# Results

Raw results are immutable JSON files under `results/raw/<dataset>/<model>/<run_id>.json` and are ignored by Git. Aggregate CSV and Markdown tables are regenerated from raw files under `results/aggregated/` and are also ignored by Git.
